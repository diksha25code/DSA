#include <iostream>
#include <algorithm>        //sort declare krne k liye use krte h
using namespace std;

int main()
{
    int a[]={0,1,2,0,2};
    int n=5;
    
    sort(a,a+n);
    for(int i=0;i<n;i++){
        cout<<a[i]<<" ";
    }
    return 0;
}

//
0 0 1 2 2
